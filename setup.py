from setuptools import setup, find_packages

setup(
    name="keiba-ai-prediction-system",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # pyproject.toml から読み込む
    ],
)
